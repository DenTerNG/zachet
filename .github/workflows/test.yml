name: my-workflow-name

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: build
      run: ls && cd .. && ls && cmake -H. -B_build -DBUILD_TESTS=ON
    - name: test
      run: cd .. && cmake --build _build --target test
