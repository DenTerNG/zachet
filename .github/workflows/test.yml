name: my-workflow-name

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: build
      run: ls 
    - name: test
      run: cd .. && cmake --build _build --target test
